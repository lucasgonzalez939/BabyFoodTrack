// Example Supabase configuration.
// You can set credentials here OR use the in-app Settings UI.
// In-app credentials (stored in localStorage) take priority over this file.
window.SUPABASE_CONFIG = {
  enabled: true,
  url: 'https://YOUR_PROJECT_ID.supabase.co',
  anonKey: 'YOUR_SUPABASE_ANON_KEY',
  profileId: '' // Leave empty to auto-generate, or set a UUID to share across devices
};
